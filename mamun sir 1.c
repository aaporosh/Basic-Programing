#include<stdio.h>
int main()
{
    int a=5,x=7,y=5,sum,sub;
    if(a%2==1)
    {
        sum=x+y;
        printf("the  sum is %d",sum);
    }
    else
    {
        sub=x-y;
        printf("the sub is %d",sub);
    }



}
